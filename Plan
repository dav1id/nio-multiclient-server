
- Clean up the server and client relationship. Right now the server can't communicate with the client when a client that
it's trying to connect to doesn't exist. Have a way to send server messages and a mental track of the clients that are
connected on the clients side

- Add a class called VersionHistory that is going to keep a history of the different chats based on the client that sent
it

- Program the css and fxml files in resources using JavaFX:
    - Creating buttons on another clients connection in the clientslist, assigning them a name and then giving some kind of
    emoji next to their name (just to be visually appealing)
    - Allow the user to send messages in the textfield, record it and then send it to the server for processing
    - Possible message persistance? Have a way to serialize the messages so that message history is not lost on client
    disconnection




 - Closing the local channel when the exit button has been pressed on JavaFX
